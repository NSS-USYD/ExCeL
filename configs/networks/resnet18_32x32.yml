network:
  name: resnet18_32x32
  num_classes: '@{dataset.num_classes}'
  pretrained: False          # set 'True' to load pretrained model
  checkpoint: results/cifar100_resnet18_32x32_base_e100_lr0.1_default/s0/best.ckpt 
  num_gpus: '@{num_gpus}'
